import { StrictMode } from "react";
import Playground from "./Playground";

const PlaygroundApp = () => {
    return (
        <StrictMode>
            <Playground />
        </StrictMode>
    );
};

export default PlaygroundApp;
